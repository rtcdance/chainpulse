version: 2

run:
  timeout: 5m

issues:
  exclude-dirs:
    - vendor
    - test/fixtures

linters:
  disable:
    - asciicheck  # Skip due to missing dependency
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused

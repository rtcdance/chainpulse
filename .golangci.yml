linters-settings:
  errcheck:
    check-type-assertions: false
  govet:
    disable:
      - fieldalignment
      - unusedwrite
linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - ineffassign
issues:
  exclude-use-default: false
  max-per-linter: 0
  max-same: 0
